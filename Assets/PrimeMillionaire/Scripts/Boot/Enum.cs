namespace PrimeMillionaire.Boot
{
    public enum BootState
    {
        None = 0,
        Load = 1,
        Login = 2,
        Restart = 3,
    }

    public enum ModalType
    {
        None = 0,
    }
}